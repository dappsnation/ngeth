{
  "$schema": "http://json-schema.org/schema",
  "title": "Serve Node",
  "description": "Serve an Ethereum node and deploy contracts defined by autoDeploy.",
  "type": "object",
  "properties": {
    "tsconfig": {
      "type": "string",
      "description": "The path to the tsconfig file used to run hardhat."
    },
    "config": {
      "type": "string",
      "description": "The path to the hardhat configuration file."
    },
    "showStackTraces": {
      "type": "boolean",
      "default": false,
      "description": "Show the stack traces."
    }
  },
  "additionalProperties": false,
  "required": ["tsconfig", "config"]
}
