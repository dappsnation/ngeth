{
  "$schema": "http://json-schema.org/schema",
  "title": "Test contract with JEST",
  "description": "Run the Hardhat test with jest.",
  "type": "object",
  "properties": {
    "tsconfig": {
      "type": "string",
      "description": "The path to the tsconfig file used to run hardhat."
    },
    "config": {
      "type": "string",
      "description": "The path to the hardhat configuration file."
    },
    "jestConfig": {
      "type": "string",
      "description": "The path to the jest configuration file."
    },
    "showStackTraces": {
      "type": "boolean",
      "default": false,
      "description": "Show the stack traces."
    },
    "watch": {
      "type": "boolean",
      "default": false,
      "description": "Watch changes."
    }
  },
  "additionalProperties": false,
  "required": ["tsconfig", "config"]
}
